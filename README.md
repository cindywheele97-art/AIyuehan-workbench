# AIyuehan Workbench

This repository is being initialized as the governance root for AIyuehan Workbench, a provider-neutral AI-Native FDE workbench.

Current state: **GOV-0/R0 public-protected v4 proposal prepared; exact-digest formal approval pending.**

- The repository contains architecture evidence and governance-only assets.
- M-1, M0, and product implementation have not started; they remain blocked until R0 activation. Installation, dynamic qualification, deployment, and release have not started.
- The public GitHub repository is `cindywheele97-art/AIyuehan-workbench` (repository database ID `1350747678`, owner database ID `284274547`).
- Active branch and tag rulesets were visible in an administrator readback while the remote still had no refs. That readback is neither signed publisher evidence nor independent pull-only attestor evidence.
- A real root key, exact-digest formal approval, role-separated signed evidence, and the protected governing tag are still absent. The repository and rules therefore remove the former capability blocker but do not activate R0.
- `CODEOWNERS` is review-routing metadata only; it is not treated as ref protection.

See [governance/README.md](governance/README.md) and [governance/r0/STATUS.md](governance/r0/STATUS.md) for the governing boundary.
